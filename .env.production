# Production build config (auto-loaded by `npm run build`, Vite bakes VITE_* vars in
# at build time). Relative path works because nginx serves the frontend and proxies
# /api/ to the backend on the same origin - see deploy/nginx/monkey.
VITE_API_BASE_URL=/api
